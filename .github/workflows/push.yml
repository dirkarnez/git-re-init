name: cpp-cmake-mingw-prebuilt-release-actions-workflow
on:
  push

jobs:
  build:
    name: Create Release
    runs-on: windows-latest
    env:
      target_repo: rust-portable
    steps:
      - shell: cmd
        run: | 
          git config --global user.name "dirkarnez" &&^
          git config --global user.email "smalldirkalex@gmail.com" &&^
          git clone --recursive https://dirkarnez:${{ secrets.GH_PAT }}@github.com/dirkarnez/${{ env.target_repo }}.git &&^
          cd "${{ env.target_repo }}" &&^
          dir &&^
          git push --delete origin v1.65.0 &&^
          git push --delete origin v1.65.0test &&^
          git push --delete origin v1.65.0test2 &&^
          git push --delete origin v1.65.0test3 &&^
          git push --delete origin v1.65.0test4 &&^
          git push --delete origin v1.65.0test5 &&^
          git push --delete origin v1.65.0test6

