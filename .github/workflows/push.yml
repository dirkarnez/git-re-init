name: cpp-cmake-mingw-prebuilt-release-actions-workflow
on:
  push

jobs:
  build:
    name: Create Release
    runs-on: windows-latest
    env:
      target_repo: lame-prebuilt
    steps:
      - shell: cmd
        run: | 
          git config --global user.name "dirkarnez" &&^
          git config --global user.email "smalldirkalex@gmail.com" &&^
          git clone --recursive https://dirkarnez:${{ secrets.GH_PAT }}@github.com/dirkarnez/${{ env.target_repo }}.git &&^
          cd "${{ env.target_repo }}" &&^
          dir &&^
          git push --delete origin v3.100test18 &&^
          git push --delete origin v3.100test17 &&^
          git push --delete origin v3.100test16 &&^
          git push --delete origin v3.100test15 &&^
          git push --delete origin v3.100test14 &&^
          git push --delete origin v3.100test13 &&^
          git push --delete origin v3.100test12 &&^
          git push --delete origin v3.100test11 &&^
          git push --delete origin v3.100test10 &&^
          git push --delete origin v3.100test9 &&^
          git push --delete origin v3.100test8 &&^
          git push --delete origin v3.100test7 &&^
          git push --delete origin v3.100test6 &&^
          git push --delete origin v3.100test5 &&^
          git push --delete origin v3.100test4 &&^
          git push --delete origin v3.100test3 &&^
          git push --delete origin v3.100test2 &&^
          git push --delete origin v3.100test
          
