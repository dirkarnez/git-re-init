name: cpp-cmake-mingw-prebuilt-release-actions-workflow
on:
  push

jobs:
  build:
    name: Create Release
    runs-on: windows-latest
    env:
      target_repo: mpc-prebuilt
    steps:
      - shell: cmd
        run: | 
          git config --global user.name "dirkarnez" &&^
          git config --global user.email "smalldirkalex@gmail.com" &&^
          git clone --recursive https://dirkarnez:${{ secrets.GH_PAT }}@github.com/dirkarnez/${{ env.target_repo }}.git &&^
          cd "${{ env.target_repo }}" &&^
          dir &&^
          git push --delete origin v1.2.1test &&^
          git push --delete origin v1.2.1test2 &&^
          git push --delete origin v1.2.1test3 &&^
          git push --delete origin v1.2.1test4 &&^
          git push --delete origin v1.2.1test5 &&^
          git push --delete origin v1.2.1test6
